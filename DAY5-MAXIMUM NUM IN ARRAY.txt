#include <stdio.h>
int main() 
{
    int i,max=0,arr[3];
    for(i=0;i<3;i++) {
        scanf("%d",&arr[i]);
        if(arr[i]>max) {
            max=arr[i];
        }
    }
    printf("max=%d",max);
}   
OUTPUT:
1
2
3
max=3